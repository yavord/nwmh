### holds all functions for .vcf annotation


annotateVariationType <- function(vcfFix) {
  # check that input is of the right type
  if(!is.character(vcfFix)) stop("vcfFix is not a character")
  
  # 
  for (i in length(vcfFix[,1])) {
    
  }
  
  return()
}